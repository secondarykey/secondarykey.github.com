sequenceDiagram
    participant User@{ "type": "actor"}  as You
    participant API@{ "type": "boundary"} as Binder
    participant Auth@{ "type": "control", "alias": "Auth Service" }
    participant DB@{ "type": "database"} as Directory
    participant Git@{ "type": "database"} as Git
    API->>Auth: Login request
    Auth->>DB: Query user
    DB-->>Auth: User data
    Auth-->>API: Access token